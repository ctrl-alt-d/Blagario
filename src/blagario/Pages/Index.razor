@page "/"
@inherits BaseIndex

<div 
    class="universe" 
    style="@Universe.CssStyle(Eyeglass)"
    @onmousemove="@TrackMouse"
>
    <div 
        class="world" 
        style="@Universe.World.CssStyle(Eyeglass)"
    >
        @foreach (var element in Universe.World.Elements )
        {
            <div class="@element.CssClass"
                style="@element.CssStyle(Eyeglass)">
            </div>
        }
    </div>
</div>
